<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme -->
    <style name="Base.Theme.EcomApp" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Default App Theme -->
        <item name="colorPrimary">@color/white</item> <!-- Toolbar color -->
        <item name="colorPrimaryDark">@color/blue</item> <!-- Status bar color -->
        <item name="colorAccent">@color/green</item> <!-- Accent color -->
    </style>

    <!-- Application theme -->
    <style name="Theme.EcomApp" parent="Base.Theme.EcomApp" />

</resources>
